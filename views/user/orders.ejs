<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>My Orders - Baeboo</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
  <style>
  body {
    background: linear-gradient(to right, #e0f7f4, #fdfdfd);
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    color: #333;
  }

  .header {
    padding: 20px 0;
    background-color: #ffffff;
    border-bottom: 1px solid #e0e0e0;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.04);
    position: sticky;
    top: 0;
    z-index: 999;
  }

  .header h2 {
    font-weight: 800;
    color: #046963;
    margin: 0;
    font-size: 28px;
  }

  .header__menu ul {
    display: flex;
    gap: 25px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .header__menu a {
    text-decoration: none;
    color: #333;
    font-weight: 500;
    transition: color 0.3s ease;
  }

  .header__menu a:hover,
  .header__menu .active a {
    color: #046963;
  }

  .orders-wrapper {
    max-width: 1024px;
    margin: 60px auto;
    background: #fff;
    border-radius: 16px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.07);
    padding: 45px 40px;
    animation: fadeIn 0.4s ease-in;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  h3 {
    text-align: center;
    color: #046963;
    font-weight: 700;
    margin-bottom: 30px;
    font-size: 26px;
  }

  .no-orders {
    background-color: #fff3cd;
    border: 1px solid #ffeeba;
    padding: 20px;
    border-radius: 10px;
    color: #856404;
    text-align: center;
    font-weight: 600;
  }

  .form-control {
    border-radius: 30px;
    padding: 10px 20px;
    border: 1px solid #ced4da;
    transition: border-color 0.3s ease;
  }

  .form-control:focus {
    border-color: #046963;
    box-shadow: 0 0 0 0.2rem rgba(4, 105, 99, 0.1);
  }

  .table {
    border-radius: 10px;
    overflow: hidden;
  }

  .table th {
    background-color: #e6fcfb;
    color: #046963;
    text-transform: uppercase;
    font-size: 14px;
    letter-spacing: 0.5px;
  }

  .table td {
    background-color: #ffffff;
    color: #444;
  }

  .table th,
  .table td {
    vertical-align: middle;
    text-align: center;
    padding: 14px 12px;
  }

  .badge {
    padding: 7px 14px;
    font-size: 0.85rem;
    border-radius: 50px;
    font-weight: 500;
    text-transform: capitalize;
  }

  .btn-outline-primary {
    border-color: #046963;
    color: #046963;
    border-radius: 25px;
    font-weight: 500;
  }

  .btn-outline-primary:hover {
    background-color: #046963;
    color: #fff;
  }

  .btn-outline-secondary {
    border-radius: 25px;
    font-weight: 500;
  }

  .pagination .page-link {
    border-radius: 50px;
    margin: 0 5px;
    color: #046963;
    border-color: #046963;
    transition: all 0.3s ease;
  }

  .pagination .page-item.active .page-link {
    background-color: #046963;
    color: #fff;
    border-color: #046963;
  }

  .pagination .page-item.disabled .page-link {
    color: #aaa;
    border-color: #eee;
  }

  /* Header navigation right side */
  .header__nav__option {
    display: flex;
    align-items: center;
    gap: 15px;
  }

  .sign-in-link {
    font-weight: 600;
    text-decoration: none;
    color: #333;
  }

  .sign-in-link:hover {
    color: #046963;
  }

  .dropdown {
    position: relative;
  }

  .dropdown-content {
    display: none;
    position: absolute;
    right: 0;
    top: 110%;
    background-color: #fff;
    border-radius: 10px;
    box-shadow: 0 8px 18px rgba(0, 0, 0, 0.08);
    z-index: 1000;
    min-width: 160px;
  }

  .dropdown-content a {
    padding: 12px 20px;
    display: block;
    color: #333;
    text-decoration: none;
    transition: background 0.3s ease;
  }

  .dropdown-content a:hover {
    background-color: #f2f2f2;
  }

  .dropdown:hover .dropdown-content {
    display: block;
  }

  .header__nav__option img {
    width: 22px;
    height: 22px;
    object-fit: contain;
  }

  .header__nav__option a {
    position: relative;
  }

  .header__nav__option a span {
    position: absolute;
    top: -8px;
    right: -10px;
    background-color: #046963;
    color: white;
    font-size: 11px;
    padding: 2px 6px;
    border-radius: 50%;
  }

  form[role="search"] .btn {
    font-weight: 600;
    min-width: 100px;
  }

  @media (max-width: 768px) {
    .orders-wrapper {
      padding: 30px 20px;
    }

    .header__menu ul {
      flex-direction: column;
      gap: 12px;
    }

    .table th,
    .table td {
      font-size: 14px;
    }
  }
  .logo-baeboo {
  font-family: 'Nunito Sans', sans-serif;
  font-size: 2.5rem;
  font-weight: 700;
  line-height: 1;
  text-transform: lowercase;
  letter-spacing: -0.05em;
  color: #222;
}

.logo-baeboo .part-boo {
  font-family: 'Nunito Sans', serif; /* or pair a serif like Playfair Display */
  font-weight: 900;
  font-size: 2.6rem;
  letter-spacing: 0;
  color: #ff7f50;
  text-shadow: 1px 1px 3px rgba(0,0,0,0.2);
}
.footer {
  font-family: 'Nunito Sans', sans-serif;
  color:black;
}
.footer-logo {
  font-size: 1.8rem;
  font-weight: 700;
  color:black;
  margin-bottom: 0.5rem;
}
.col-md-3 h6 {
  font-size: 1.25rem;
  font-weight: 700;
  color: #222;
  margin-bottom: 0.75rem;
  letter-spacing: 0;
  text-transform: uppercase;
}
.col-md-2 h6 {
  font-size: 1.25rem;
  font-weight: 700;
  color: #222;
  margin-bottom: 0.75rem;
  letter-spacing: 0;
  text-transform: uppercase;
}

.footer-tagline {
  font-size: 1rem;
  color:black;
}
.footer-list {
  list-style: none;
  padding: 0;
}
.footer-list li {
  margin-bottom: 0.5rem;
}
.footer-list a {
  color:black;
  text-decoration: none;
  transition: color .3s;
}
.footer-list a:hover {
  color: #ff7f50;
}
.social-icons {
  display: flex;
  gap: 0.75rem;
}
.social-icons a {
  color:black;
  font-size: 1.2rem;
  transition: color .3s;
}
.social-icons a:hover {
  color: #ff7f50;
}
.footer .border-top {
  border-color: #ddd;
}
</style>

</head>

<body>
  <!-- Header -->
  <header class="header">
    <div class="container d-flex justify-content-between align-items-center">
      <h2 class="logo-baeboo">
  <span class="part-bae">bae</span><span class="part-boo">boo</span>
</h2>

      <nav class="header__menu">
        <ul class="d-flex gap-4 list-unstyled mb-0">
          <li class="active"><a href="/">Home</a></li>
          <li><a href="/shop">Shop</a></li>
        </ul>
      </nav>

      <!-- User Section -->
      <div class="header__nav__option d-flex align-items-center gap-3">
        <% if (user) { %>
          <div class="dropdown">
            <a href="#" class="sign-in-link">
              <%= user.name %>
            </a>
            <div class="dropdown-content">
              <a href="/userProfile">Profile</a>
              <a href="/logout">Logout</a>
            </div>
          </div>
          <% } else { %>
            <a href="/signup" class="sign-in-link">Sign Up</a>
            <a href="/login" class="sign-in-link">Log in</a>
            <% } %>

              <a href="/wishlist"><img src="/img/icon/heart.png" alt=""></a>
              <a href="/cart"><img src="/img/icon/cart.png" alt=""> <span>0</span></a>
      </div>
    </div>
  </header>


  <!-- Orders Section -->
  <div class="container">
    <div class="orders-wrapper">
      <h3>My Orders</h3>

      <!-- Search Bar -->
      <form method="GET" action="/orders" role="search" class="mb-4 d-flex gap-2">
        <input type="text" name="search" placeholder="Search orders by ID, status or name"
          value="<%= typeof search !== 'undefined' ? search : '' %>" class="form-control flex-grow-1" />

        <button type="submit" class="btn btn-outline-primary px-3">
          <i class="bi bi-search"></i> Search
        </button>

        <% if (search && search.trim().length> 0) { %>
          <a href="/orders" class="btn btn-outline-secondary px-3">
            <i class="bi bi-x-circle"></i> Clear
          </a>
          <% } %>
      </form>

      <% if (orders.length===0) { %>
        <div class="no-orders">You have no orders yet.</div>
        <% } else { %>
          <div class="table-responsive">
            <table class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Order ID</th>
                  <th>Items</th>
                  <th>Status</th>
                  <th>Placed On</th>
                  <th>Total Price (₹)</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% orders.forEach(order=> { %>
                  <tr>
                    <td>
                      <%= order.orderId %>
                    </td>
  <td>
  <% if (Array.isArray(order.orderedItems)) { %>
    <% order.orderedItems.forEach(item => { %>
      <div class="d-flex align-items-center gap-2 mb-2">
        <div class="text-start">
          <div><%= item.product?.productName ?? 'Unnamed Product' %></div>
          <div class="text-muted" style="font-size: 0.9rem;">Qty: <%= item.quantity ?? 0 %></div>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <div>No items</div>
  <% } %>
</td>
   <td>
  <% if (Array.isArray(order.orderedItems)) { %>
    <% order.orderedItems.forEach(item => { %>
      <div class="mb-2">
        <% if (item.cancelStatus === 'cancelled') { %>
          <span class="badge bg-danger">Cancelled</span>
        <% } else if (item.returnStatus === 'requested') { %>
          <span class="badge bg-warning">Return Requested</span>
        <% } else if (item.returnStatus === 'approved') { %>
          <span class="badge bg-danger">Returned</span>
        <% } else if (item.returnStatus === 'rejected') { %>
          <span class="badge bg-success">Delivered</span>
        <% } else if (item.status === 'placed') { %>
          <span class="badge bg-info">Placed</span>
        <% } else if (item.status === 'shipped') { %>
          <span class="badge bg-primary">Shipped</span>
        <% } else if (item.status === 'delivered') { %>
          <span class="badge bg-success">Delivered</span>
        <% } else { %>
          <span class="badge bg-secondary">placed</span>
        <% } %>
      </div>
    <% }); %>
  <% } %>
</td>


                    <td>
                      <%= order.createdAt.toLocaleDateString() %>
                    </td>
                    <td>₹<%= (order.finalAmount ?? 0).toFixed(2) %>
                    </td>
                    <td>
                      <a href="/order/<%= order.orderId %>" class="btn btn-sm btn-outline-primary">View</a>
                    </td>
                  </tr>
                  <% }); %>
              </tbody>
            </table>
          </div>

          <!-- Pagination -->
          <nav class="mt-4">
            <ul class="pagination justify-content-center">
              <% if (currentPage> 1) { %>
                <li class="page-item">
                  <a class="page-link"
                    href="?page=<%= currentPage - 1 %><%= search ? '&search=' + encodeURIComponent(search) : '' %>">&laquo;</a>
                </li>
                <% } else { %>
                  <li class="page-item disabled"><span class="page-link">&laquo;</span></li>
                  <% } %>

                    <% for (let i=1; i <=totalPages; i++) { %>
                      <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                        <a class="page-link"
                          href="?page=<%= i %><%= search ? '&search=' + encodeURIComponent(search) : '' %>">
                          <%= i %>
                        </a>
                      </li>
                      <% } %>

                        <% if (currentPage < totalPages) { %>
                          <li class="page-item">
                            <a class="page-link"
                              href="?page=<%= currentPage + 1 %><%= search ? '&search=' + encodeURIComponent(search) : '' %>">&raquo;</a>
                          </li>
                          <% } else { %>
                            <li class="page-item disabled"><span class="page-link">&raquo;</span></li>
                            <% } %>
            </ul>
          </nav>
          <% } %>
    </div>
  </div>
   <%-include("../../views/partials/user/footer.ejs")%>
</body>

</html>