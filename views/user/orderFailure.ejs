<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Order Failed</title>
  <link rel="stylesheet" href="/css/styles.css"> <!-- Your CSS file -->
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      text-align: center;
      padding: 50px;
      background-color: #f9f9f9;
    }
    .success-container {
      max-width: 600px;
      margin: auto;
      background: white;
      padding: 40px;
      border-radius: 20px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    }
    .success-image {
      width: 200px;
      margin-bottom: 20px;
    }
    h1 {
      color: #28a745;
      font-size: 2.5rem;
    }
    p {
      font-size: 1.2rem;
      margin: 20px 0;
    }
    .btn-group {
      margin-top: 30px;
    }
    .btn {
      padding: 12px 24px;
      font-size: 1rem;
      border: none;
      border-radius: 8px;
      margin: 10px;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
    }
    .btn-primary {
      background-color: #007bff;
      color: white;
    }
    .btn-shop {
      background-color: #ffc107;
      color: black;
    }
    .btn:hover {
      opacity: 0.9;
    }
  </style>
</head>
<body>
  <div class="failure-container">
    <h1>Payment Failed</h1>
    <p>Unfortunately, your payment could not be completed</p>
    <div class="btn-group">
      <a href="/retry-payment/<%= orderId%>" class="btn btn-primary">Retry payment</a>
      <a href="/orders" class="btn btn-shop">View Order</a>
    </div>
  </div>
</body>
</html>
